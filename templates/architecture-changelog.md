# Architecture Changelog

Track major structural changes to the solution. Each entry records what changed, why, and the metric impact. Used by `/perf-lab:replay` to identify experiments worth retrying.

<!-- Append new entries at the top. Format:
### vN: Short description (experiment #X)
- Date: YYYY-MM-DD
- Cycles: before → after
- Change: What was done
- Binding constraint shifted: from → to
- Replay candidates generated: [count or "pending"]
-->
